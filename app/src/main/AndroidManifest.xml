<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.abhishek.stylesnsmiles">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Splashscreen"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Home" />
        <activity
            android:name=".Login"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".LoginUser"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity android:name=".Registeration" />
        <activity android:name=".ParlourPage" />
        <activity android:name=".ParlourRegister" />
        <activity android:name=".ParlourDetails" />
        <activity android:name=".ViewBeaticianList" />
        <activity android:name=".BookingAppointment" />
        <activity android:name=".ChatScreen"></activity>
    </application>

</manifest>